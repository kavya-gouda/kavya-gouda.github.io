# kavya-gouda.github.io

Blog starts at `index.html`.

To add a post:
1. Create `posts/<slug>/post.md` (first `# Heading` becomes the title)
2. Let the auto-action regenerate `posts.json` (or run it by pushing changes)